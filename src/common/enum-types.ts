export enum NotificationType {
  CreateMoment = 0,
  UpdateMoment = 1,
  CreateComment = 2,
}

export enum SignInType {
  Web = 0,
  Mobile = 1,
}

export enum SocialSignInType {
  Google = 0,
  Naver = 1,
}
